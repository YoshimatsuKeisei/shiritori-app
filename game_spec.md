# しりとりアプリ ゲーム仕様書

## 1. 目的

本アプリは、通常のしりとりをベースに複数の特殊ルールを組み合わせて遊ぶ、短時間・対戦型の「変則しりとりゲーム」である。

単純な語彙量だけではなく、制限時間内の発想力、ルール適応力、文字数管理、語彙選択を競う。

初期実装では 1 vs 1 を主対象とするが、将来的に3人以上の脱落戦へ拡張可能な構造とする。

第7段階ではUI非依存の1vs1状態を実装済みとする。2人はランダム先攻で交互に回答し、個別持ち時間を持つ。辞書ロード中は時計停止、不正解・漢字候補選択中は時計継続とする。`END_WITH_N`、`TIMEOUT`、`NO_VALID_WORD`の時点で相手の勝利を確定する。使用済み語は両プレイヤーで共有する。

第8段階では通常起動を1vs1 React MVPとする。開始設定で試合形式、制約オプション、両者共通の1〜10分または1〜60分のカスタム持ち時間、スキップ有無を選択する。画面には双方の残り時間・ターン・スキップ残数、回答履歴、接続条件を表示する。終了後は勝敗、敗北理由、回答数、平均時間、平均文字数、最長語、残り時間、使用スキップ数を表示し、同設定で再戦または設定へ戻れる。

---

## 2. 基本方針

- ユーザー入力はすべて **ひらがな** で完結させる。
- 使用可能語は原則として **名詞** とする。
- **ことわざ** は例外として使用可能。
- 一般語は主に **JMdict**、固有名詞は主に **JMnedict** を情報源とする。
- 漢字限定時のみ、ひらがな入力後に漢字候補を表示し、ユーザーが意図した表記を選択する。
- カタカナ限定時は、ひらがな入力に対応するカタカナ表記が辞書に存在するかで判定する。
- 不正解入力は即敗北にせず、タイマーを止めずに再入力可能とする。
- ただし **語尾が「ん」になった正解語を送信した場合は即敗北** とする。
- ルール構造は「ゲーム設定」「試合形式」「制約オプション」の3階層に分ける。
- 1ゲームにつき試合形式を必ず1つ選び、形式ごとの上限内で制約オプションを追加できる。
- タイマー、スキップ、プレイヤー人数、辞書範囲などはゲーム設定とし、制約オプション数には含めない。

---

## 3. 使用文字・文字数

### 3.1 文字数の数え方

表示上の仮名1文字を1文字として数える。

例：

- きゃく → 3文字
- きゃ → 2文字
- っき → 2文字
- スーパー → 4文字
- すーぱー → 4文字
- ゔぁいおりん → 6文字

以下もすべて1文字として扱う。

- 小書き文字（ゃ、ゅ、ょ、ぁ、ぃ、ぅ、ぇ、ぉ、っ 等）
- ゔ
- 長音記号「ー」

### 3.2 文字数計算から除外するもの

以下は文字数に含めない。

- 空白
- 中黒「・」
- 句読点
- 記号類
- その他、語そのものの仮名列に含めない装飾文字

ただし「ー」は文字数に含める。

---

## 4. 基本しりとり

### 4.1 通常ルール

前の有効語の末尾1文字と、次の語の先頭1文字を一致させる。

例：

りす → すいか → からす

### 4.2 「ん」の扱い

語尾が「ん」の単語を確定した時点で、そのプレイヤーは即敗北・脱落する。

どの試合形式でも例外としない。

### 4.3 開始文字

ゲーム開始時の開始文字はランダムに決定する。

- 小文字や「ー」を開始文字候補に含めない。
- 通常かな・濁音・半濁音を候補として利用可能。
- 「ん」を開始文字にするかどうかは実装時に設定値として管理可能にする。

初期実装では、成立しやすさを優先して「ん」は開始文字候補から除外してよい。

---

## 5. ゲーム設定

ゲーム設定は制約オプション数のカウント対象外とする。

### 5.1 タイマー

各プレイヤーが個別の持ち時間を持つ方式を基本とする。

選択肢：

- 1分
- 2分
- 3分
- 4分
- 5分
- 6分
- 7分
- 8分
- 9分
- 10分
- カスタム（分単位）

自分のターン中のみ残り時間が減少し、次ターンへ持ち越す。

以下の間もタイマーは停止しない。

- 不正解後の再入力
- 漢字候補一覧の閲覧
- 漢字候補の検索
- 漢字候補のスクロール

### 5.2 スキップ

補助的なゲーム設定として、1ゲームにつき1回のスキップを設定可能とする。

スキップ回数はプレイヤーごとに管理する。`GROWING_LENGTH`ではスキップを使用不可とする。

スキップ時：

- プレイヤーは敗北しない。
- 現在の条件を次のプレイヤーへ引き継ぐ。
- 使用済み回数を1消費する。
- スキップ自体は有効回答には数えない。

### 5.3 辞書範囲

初期候補：

- 一般名詞
- 固有名詞
- ことわざ
- 人名
- 地名
- 組織名
- 作品名
- その他 JMnedict の分類

「有名かどうか」は判定基準に使用しない。

---

## 6. 試合形式と制約オプション

### 6.1 試合形式

1ゲームにつき以下から必ず1つ選ぶ。

| 試合形式 | 識別子 | 制約オプション上限 |
| --- | --- | ---: |
| 通常しりとり | `NORMAL` | 3 |
| 2文字しりとり | `TWO_CHARACTER` | 2 |
| 1文字ずつ必要文字数が増えるしりとり | `GROWING_LENGTH` | 0 |
| カテゴリマスター | `CATEGORY_MASTER` | 2 |
| リバースしりとり | `REVERSE` | 2 |
| 禁止文字しりとり | `FORBIDDEN_CHARACTER` | 2 |

カテゴリマスターのカテゴリ判定はカテゴリ基盤導入後に実装する。

### 6.2 制約オプション

- 漢字オンリー（`KANJI_ONLY`）
- カタカナオンリー（`KATAKANA_ONLY`）
- 前の語の最後の漢字を使用（`REQUIRED_LAST_KANJI`）
- 前の語の最後の漢字の部首を使用（`REQUIRED_LAST_KANJI_RADICAL`）

漢字オンリーとカタカナオンリーは排他とする。末尾漢字・同部首オプションは漢字オンリーを必須とし、末尾漢字と同部首は同時使用不可とする。

詳細は `rule_engine_spec.md` を参照。

---

## 7. 不正解入力

不正解は原則として即敗北にしない。

不正解例：

- 辞書に存在しない
- 名詞・ことわざではない
- 使用済み
- 指定文字数違反
- 禁止文字を含む
- しりとり接続条件を満たさない
- 漢字限定条件を満たさない
- カタカナ限定条件を満たさない
- 部首条件を満たさない

処理：

1. NG理由を表示
2. 入力欄へ戻す
3. タイマーは継続
4. 再入力可能

例外：

- 正解として確定可能な語が「ん」で終わる → 即敗北
- 持ち時間が0になる → 即敗北
- 現在条件を満たす語が辞書上0件 → 即敗北

候補0件の場合の救済は行わない。

---

## 8. 使用済み判定

### 8.1 通常・カタカナ等

同じ読みは使用済みとして扱う。

例：

「はし」を一度使用した場合、通常モードでは橋・箸・端など同音異義語も含めて再使用不可。

### 8.2 漢字限定

漢字表記が異なる場合は別単語として扱う。

例：

- 橋
- 箸
- 端

は、それぞれ別の使用済みIDを持つ。

ただし、同一語の単なる表記揺れ・異体字等については辞書正規化ルールで同一語IDへ束ねられる余地を持つ。

---

## 9. 対戦人数

### 9.1 初期実装

1 vs 1 を主対象とする。

### 9.2 将来拡張

3人以上の場合は脱落戦方式を採用する。

- ゲーム開始時にランダムシャッフル
- 1人脱落してもゲーム継続
- 脱落者を手番リストから除外
- 最後の1人になるまで続行

例：

A → B → C → D

B脱落後：

A → C → D → A → ...

---

## 10. 脱落

主な脱落理由：

- `END_WITH_N` : 「ん」で終了
- `TIMEOUT` : 持ち時間切れ
- `NO_VALID_WORD` : 条件を満たす語が辞書上0件
- 将来拡張用のその他理由

「ん」で脱落したプレイヤーの語は有効語として盤面に残さず、直前の有効語から次プレイヤーが続行する。

---

## 11. 順位

基本順位は脱落順で決定する。

最後まで生存したプレイヤーが1位。

同着判定が必要な場合は、以下の順で比較する。

1. 平均回答時間が短い
2. 平均使用文字数が長い
3. 有効回答数が多い

### 記録する統計

- 有効回答数
- 総回答時間
- 平均回答時間
- 総使用文字数
- 平均使用文字数
- 最長単語
- スキップ使用有無
- 脱落理由

---

## 12. ゲーム開始

- 先攻はランダム
- 3人以上ではプレイヤー順をランダムシャッフル
- 開始文字もランダム
- 最初のプレイヤーからタイマー開始
- 最初の回答にも選択した試合形式と制約オプションをすべて適用する

---

## 13. 今後の拡張候補

本仕様には含める余地を残すが、初期実装必須ではない。

- テーマ縛り
- Japanese WordNet 等を使った意味カテゴリ
- オンライン対戦
- CPU対戦
- 戦績保存
- ランキング
- ルールプリセット
- 辞書難易度
- マニアック語許可設定
